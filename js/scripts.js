/*
wow = new WOW({//basic initiation of wow plugin
  boxClass:     'wow',      // default
  animateClass: 'animated', // default
  offset:       0,          // default
  mobile:       true,       // default
  live:         true        // default
})wow.init();

*/ 
 


/*
$( document ).ready(function() { //when document is ready
    
    $('nav').slicknav({ //basic usage of slicknav menu
        label: '',
	prependTo:'.mobile-menu'
    });
    
    $(".toTop").click(function(){//basic usage of scroll-top plugin

        $("body").scrollTo(0,'slow',{axis:'y'});

    });

   $('.owl-carousel').owlCarousel({
		oop:true,
	 	margin:10,
	    	nav:true
   	
   });
    
    
});
*/


/*
$(window).scroll(function(){ //on window scroll function
        
});
*/
